# Supabase — from the project's API settings.
# The anon key is public by design; it is safe in the client bundle because
# row level security is what actually protects the data. Never put the
# service_role key in anything prefixed NEXT_PUBLIC_.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
